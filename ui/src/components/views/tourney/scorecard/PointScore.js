import React from "react";
import { Typography } from "@mui/material";

// TODO - When we start using this, the Table component needs to keep state (?)
export default function PointScore({ par }) {
  return (
    <div>
      <Typography variant="subtitle1">{}</Typography>
    </div>
  );
}
